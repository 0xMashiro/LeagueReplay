// Window-level mode switches. CSS follows the rendered mode; component
// reflow belongs in container queries rather than these viewport breakpoints.
export const compactSidebarQuery = "(max-width: 1100px)";
export const inlineNotesQuery = "(min-width: 1400px)";
